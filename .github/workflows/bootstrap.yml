name: Puzzle Arcade Deployment Staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Deployment payload staging only'
